# AgentKern Identity Docker Compose
# Usage: docker compose up -d

services:
  # ========================
  # AgentKern Identity API
  # ========================
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - PORT=3001
      - DATABASE_HOST=postgres
      - DATABASE_PORT=5432
      - DATABASE_USER=agentkern
      - DATABASE_PASSWORD=agentkern_secret
      - DATABASE_NAME=agentkern_identity
      - DATABASE_SYNC=false
      - DATABASE_SSL=false
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_PASSWORD=redis_secure_password_change_me
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3001/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    restart: unless-stopped
    networks:
      - agentkern-network

  # ========================
  # PostgreSQL Database
  # ========================
  postgres:
    image: postgres:16-alpine
    environment:
      - POSTGRES_USER=agentkern
      - POSTGRES_PASSWORD=agentkern_secret
      - POSTGRES_DB=agentkern_identity
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U agentkern -d agentkern_identity"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - agentkern-network

  # ========================
  # Redis (for caching/sessions)
  # ========================
  redis:
    image: redis:7-alpine
    command: redis-server --requirepass redis_secure_password_change_me --appendonly yes
    ports:
      - "6379:6379"
    environment:
      - REDIS_PASSWORD=redis_secure_password_change_me
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - agentkern-network

volumes:
  postgres_data:
  redis_data:

networks:
  agentkern-network:
    driver: bridge
