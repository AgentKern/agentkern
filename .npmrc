# Ignore optional peer dependencies
# These are dev tools (eclint, evalmd, etc.) that aren't required at runtime
# They're listed as optional peer dependencies but don't need to be installed
legacy-peer-deps=false
strict-peer-deps=false

# Suppress warnings about missing optional peer dependencies
audit-level=moderate

# Don't fail on missing optional dependencies during npm-ls
fund=false
audit=false

