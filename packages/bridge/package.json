{
  "name": "@agentkern/bridge",
  "version": "0.1.0",
  "main": "index.node",
  "scripts": {
    "build": "napi build --platform --release",
    "build:debug": "napi build --platform"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.18.4"
  }
}
