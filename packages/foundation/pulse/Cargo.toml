[package]
name = "agentkern-pulse"
version = "0.1.0"
edition = "2024"
description = "AgentKern pulse: Unified observability and health reporting"
license = "Apache-2.0"

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
chrono = { workspace = true }
prometheus = "0.13"
lazy_static = "1.5"
tracing = { workspace = true }
async-trait = { workspace = true }
rust_decimal = { workspace = true }
