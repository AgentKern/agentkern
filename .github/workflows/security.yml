name: Security & Supply Chain

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]

jobs:
  # ============================================
  # SBOM Generation (SLSA Level 4)
  # ============================================
  sbom:
    name: Generate SBOM
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v6
      
      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
      
      - name: Install cargo-sbom
        run: cargo install cargo-sbom
      
      - name: Generate SBOM for Gate
        working-directory: packages/gate
        run: |
          cargo sbom --output-format cyclone_dx_json_1_6 > gate-sbom.json
      
      - name: Generate SBOM for Synapse
        working-directory: packages/synapse
        run: |
          cargo sbom --output-format cyclone_dx_json_1_6 > synapse-sbom.json
      
      - name: Generate SBOM for Arbiter
        working-directory: packages/arbiter
        run: |
          cargo sbom --output-format cyclone_dx_json_1_6 > arbiter-sbom.json
      
      - name: Upload SBOM artifacts
        uses: actions/upload-artifact@v6
        with:
          name: sbom
          path: |
            packages/gate/gate-sbom.json
            packages/synapse/synapse-sbom.json
            packages/arbiter/arbiter-sbom.json

  # ============================================
  # Sigstore/Cosign Signing
  # ============================================
  sign:
    name: Sign Artifacts
    runs-on: ubuntu-latest
    needs: [sbom]
    if: startsWith(github.ref, 'refs/tags/v')
    
    permissions:
      id-token: write
      contents: read
    
    steps:
      - uses: actions/checkout@v6
      
      - name: Install Cosign
        uses: sigstore/cosign-installer@v4
      
      - name: Download SBOM
        uses: actions/download-artifact@v4
        with:
          name: sbom
          path: sbom/
      
      - name: Sign SBOMs with Cosign (keyless)
        run: |
          cosign sign-blob sbom/packages/gate/gate-sbom.json --output-signature sbom/packages/gate/gate-sbom.sig
          cosign sign-blob sbom/packages/synapse/synapse-sbom.json --output-signature sbom/packages/synapse/synapse-sbom.sig
          cosign sign-blob sbom/packages/arbiter/arbiter-sbom.json --output-signature sbom/packages/arbiter/arbiter-sbom.sig
      
      - name: Upload signed artifacts
        uses: actions/upload-artifact@v6
        with:
          name: sbom-signed
          path: sbom/

  # ============================================
  # Security Audit
  # ============================================
  security-audit:
    name: Security Audit
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v6
      
      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
      
      - name: Install cargo-audit
        run: cargo install cargo-audit
      
      - name: Audit Rust workspace dependencies
        run: cargo audit
        continue-on-error: true
      
      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Audit npm dependencies
        working-directory: packages/sdk
        run: npm audit --audit-level=high
        continue-on-error: true

  # ============================================
  # License Compliance
  # ============================================
  license-check:
    name: License Compliance
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v6
      
      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
      
      - name: Install cargo-deny
        run: cargo install cargo-deny
      
      - name: Check licenses (workspace)
        run: cargo deny check licenses

